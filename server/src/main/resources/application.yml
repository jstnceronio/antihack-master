spring:
  datasource:
    url: jdbc:mysql://localhost:3306/students_db
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true